spring:
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
    show-sql: true
    properties:
      hibernate:
        connection.characterEncoding: UTF-8
        connection.useUnicode: true